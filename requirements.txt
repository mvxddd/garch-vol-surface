# Core numerics
numpy>=1.26
pandas>=2.1
scipy>=1.11

# Volatility modelling
arch>=6.3            # GARCH / EGARCH / GJR maximum-likelihood estimation
statsmodels>=0.14    # Ljung-Box, ARCH-LM diagnostics

# Market data
yfinance>=0.2.40     # prices + option chains (free tier)
requests>=2.31       # Polygon REST client
pyarrow>=14.0        # parquet cache

# Visualisation
matplotlib>=3.8
plotly>=5.18         # interactive 3-D surface

# Web interface (optional — only needed for `streamlit run app.py`)
streamlit>=1.30

# Development (optional)
pytest>=7.4
